# Remaining software acceptance closure

Source baseline: `2516bdb9970699021523e181ea3c7c4543780bbb`. Work in the isolated closure checkout while the baseline's complete regression and CI run unchanged.

The user requests all PRD functions and acceptance before promotion. Keep fixed downward layout, English first, SQL/script nodes, explicit data mappings, simple form schedules and persistent Mac background operation. No physical OS/power changes or external notifications are incidental verification.

## Bounded implementation groups

1. Browser repair: persist linked validation errors across reopening and publication failures; explicitly repair deleted mappings; verify initial-login card; require schedule fields; verify full 205-row pagination and honest empty output.
2. Portable data: run exact native type/absence/numeric/date/object and no-fanout oracles; enforce supported values; verify inclusive inline boundary, automatic complete spill and full downstream data. Preserve the conflicting historical artifact oracle as superseded and add its explicit replacement.
3. Scheduling/lifecycle: expose authorized missed-occurrence history without secrets; deduplicate restart records; verify skip/latest/grace and actual running overlap. Expose uncertain effects before an explicit new business run.
4. Execution integrity: reject unstable missing interval anchors, distinguish deterministic output/validation failures from retryable failures, and bound process logs without changing structured data. Add failing tests for every demonstrated defect before fixes.

Independent agents may implement groups with explicitly disjoint function/file ownership; root owns integration, schedule calculator, execution policy and the history/rerun UI. Do not edit the original checkout under regression.

## Verification and delivery

- Run focused RED/GREEN regressions, then full same-revision software, real n8n/language, browser, external SQL and Compose suites.
- Join JUnit results to exact reviewed case mappings; partial coverage and manual gates never count as passes.
- Preserve original case oracles when documenting a substantive clarification; identify replacements and source rationale.
- Rebuild the Mac preview after final source changes. Ad-hoc signing is not notarization.
- Physical sleep/battery/login/reboot and clean-device/distribution gates remain explicit until actually performed. Promotion assessment waits for acceptance.
