# Optional configuration. Copy this file to .env and keep real values out of Git.
# The default web port binds only to 127.0.0.1.
TASK_CONSOLE_IMAGE_TAG=local
# Change this before expanding network access beyond the local Compose network.
POSTGRES_PASSWORD=replace-with-a-local-database-password
